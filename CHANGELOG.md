# Changelog

## Unreleased

- **新增交付前機械預檢 `skills/hfg/scripts/hfg_preflight.py`（第三輪實戰後的修正，2026-09-18）**。
  第二輪補完 A2／A3／E 之後，數字類判準錯誤歸零，但 9 份規劃書裡有 4 份栽在 `old_string` 縮排
  （fenced code 憑手感重打、或 fence 縮在 bullet 底下造成「照原樣」與「扣掉 fence 縮排」兩種解讀）。
  兩位規劃者已各自手寫過一次「把每個 old_string 對目標檔 grep 一遍」的腳本，那兩次施工都零卡點，
  所以固定成工具：依序把每步的 old/new 模擬套用到檔案副本上、完整多行逐字比對，
  回報 `OK`／`INDENT`／`MISSING`／`DUPLICATE`／`SKIP`；`--out` 寫出模擬結果供 A2 dry-run；
  `--applied` 在續作時先看 `new_string` 再看 `old_string` 判斷每步做了沒。稽核清單新增 `A0` 指向它。
- **偵察標準從五條增為六條**：`old_string` 一律用 `sed -n` 從檔案貼、fence 放第 0 欄、內容用絕對縮排；
  新增標號／節號前先查下一個可用值。
- **產出流程第 7 步新增條件式獨立 reviewer**：只在有不可逆步驟、改到資料口徑或使用者要求時派，
  只給原始需求、第 1／5 節與 diff，不給 builder 報告與第 6 節；要它審判準是否量錯東西（A3）。
- **施工中的意外處理新增情境三**（session 中斷／完工報告對不上磁碟）：先查磁碟、先看 `new_string`
  再看 `old_string`、驗收基準檔要放在專案內不放 temp。
- **新增「跨模型覆核」一節**：使用者要求時，從既有規劃書與施工紀錄拼出交接材料，對方只挑錯不重做。
- **builder 契約補一句**：Edit 失敗一次就算找不到，縮排差 2 格也一樣，不准 `cat -A` 後自行修正再試。
- 模板第 5 節「理由」欄改為「理由（含為何不選另一案）」。
- 新增 `tests/test_preflight.py`。

- **強化事實稽核（第二輪實戰後的修正）**。上一版的稽核清單只要求「每個事實都是某條指令的
  輸出」，但實測第二輪仍有三次施工中斷落在同一個位置——因為**「有指令輸出」不等於
  「量對了東西」**：那些指令量的是另一個東西（另一支腳本的替換結果、一支對特定語法有 bug 的
  範圍計算），而不是執行者將要面對的那個狀態。新增：
  - `A2. 每一條「預期輸出是精確數字」的驗收，都必須 dry-run` — 拿規劃書自己寫的 `new_string`
    跑驗收表裡那條一模一樣的指令，用實際輸出回填，不准心算或沿用別處的數字。
  - `A3. 判準要對準「性質」，不是「副產品」` — 判準脆弱就代表它量錯了東西。
  - `E. 修訂稽核` — 每次改動規劃書之後重跑一次覆蓋稽核。修訂跟初次撰寫一樣會引入錯誤，
    但初次撰寫有稽核關卡、修訂沒有。
- **偵察標準從四條增為五條**：新增「工具算出來的範圍要印出首尾行做人眼複核」。
  大括號配對對無大括號 arrow function、IIFE、物件字面值方法都是壞的——實例中一個 2 行的
  常數被算成 28 行，而規劃書照那個假範圍寫「整段含上方註解刪除」會誤刪一段還在服務
  其他活函式的共用註解。
- **寫作要求新增三條**：刪除類步驟一律逐字級不准降級（邊界就是全部的風險，而逐字級會逼
  規劃者真的去看那段內容）；規劃書裡寫的腳本一律「先檢查後寫檔」；自己寫進規劃書的內容
  若有數量約束要當場數。
- 明確寫入「**執行者停下來確認不算解決問題**」——安全網生效不等於規劃做對了，
  目標是讓錯誤在規劃階段就現形。
- 修掉一處去識別化漏網（視覺任務分類表裡殘留的領域特定詞彙）。
- 新增零第三方依賴的 Python 標準函式庫品質測試。
- 新增 GitHub Actions CI，在 Python 3.10–3.13 執行測試與 `git diff --check`。
